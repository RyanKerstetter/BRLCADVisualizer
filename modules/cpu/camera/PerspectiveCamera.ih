#include "camera/Camera.ih"

OSPRAY_BEGIN_ISPC_NAMESPACE

SYCL_EXTERNAL void PerspectiveCamera_initRay(const Camera *uniform,
    varying Ray &,
    varying RayCone &,
    const varying CameraSample &,
    const uniform FeatureFlagsHandler &);

SYCL_EXTERNAL void PerspectiveCamera_initRayMB(const Camera *uniform,
    varying Ray &,
    varying RayCone &,
    const varying CameraSample &,
    const uniform FeatureFlagsHandler &);

OSPRAY_END_ISPC_NAMESPACE

